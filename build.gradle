apply plugin: 'application'

mainClassName = 'Main'

repositories {
    jcenter()
}

dependencies {
	compile 'com.graphhopper:graphhopper-reader-osm:+'
    testImplementation 'junit:junit:+'
}
